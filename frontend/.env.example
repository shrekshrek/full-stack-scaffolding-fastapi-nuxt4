# 前端环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# ===== 认证配置 =====

# Nuxt Auth Utils Session 密码（必需）
# 用于加密session cookies的密码，生产环境必须使用强密码
# 要求：至少32个字符
# 生成强密码命令：openssl rand -base64 32
# 示例：NUXT_SESSION_PASSWORD=your-very-secure-password-at-least-32-characters-long
NUXT_SESSION_PASSWORD=your-session-password-must-be-at-least-32-chars

# ===== API配置 =====

# API 基础地址（可选）
# 定义前端应用请求API时的基础路径
# 开发环境：通过 nuxt.config.ts 的 nitro.devProxy 配置自动代理到 http://localhost:8000
# 生产环境：配置实际的后端API地址或使用反向代理路径
# 默认值：/api/v1
NUXT_PUBLIC_API_BASE=/api/v1

# ===== 已废弃配置（迁移完成后可删除）=====

# 旧版 Nuxt Auth 来源 URL（已废弃）
# 从 @sidebase/nuxt-auth 迁移到 nuxt-auth-utils 后不再需要
# NUXT_AUTH_ORIGIN=http://localhost:3000 