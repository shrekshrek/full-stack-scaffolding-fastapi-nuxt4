# 前端环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# API 基础地址配置
# 这个变量定义了前端应用在浏览器中请求API时的基础路径。
# 在本地开发中, 它通常指向一个代理路径, 由 nuxt.config.ts 中的 devProxy 配置处理。
# 默认值: /api/v1
NUXT_PUBLIC_API_BASE=/api/v1

# Nuxt Auth 来源 URL
# 这个变量告诉 nuxt-auth 认证流程的来源地址。
# 通常是你的前端应用的访问地址。
NUXT_AUTH_ORIGIN=http://localhost:3000 