# 本文件是生产环境 .env 文件的模板.
# 在真实的生产环境中, 这些值应该被安全地管理,
# 例如, 通过一个密钥管理系统 (如 AWS Secrets Manager, HashiCorp Vault)
# 并在运行时注入到环境变量中.
#
# 不要将真实的 .env.production 文件提交到版本控制中.

# --- PostgreSQL 数据库 (生产环境) ---
# 使用你生产数据库的真实 IP 或主机名.
# 确保密码是强壮且被安全管理的.
DATABASE_URL=postgresql+psycopg://prod_user:a_very_strong_password@prod_db_host:5432/prod_db

# --- Redis (生产环境) ---
# 使用你生产 Redis 实例的真实 IP 或主机名.
REDIS_URL=redis://prod_redis_host:6379/1

# --- Celery (生产环境) ---
# 确保这些指向生产环境的 Redis 实例.
CELERY_BROKER_URL=redis://prod_redis_host:6379/0
CELERY_RESULT_BACKEND=redis://prod_redis_host:6379/0

# --- 安全性 (生产环境) ---
# 这必须是一个强壮、唯一且被安全存储的密钥.
# 它应该与开发环境的密钥不同.
SECRET_KEY=change_this_to_a_long_random_and_secret_string_for_production

# 生产环境的 Token 过期时间 (分钟)
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=43200 # 30 天

# --- 邮件设置 (生产环境) ---
# 使用一个真实的 SMTP 服务商, 如 SendGrid, Amazon SES 等.
SMTP_HOST=smtp.your_provider.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
EMAILS_FROM_EMAIL=noreply@your_domain.com
EMAILS_FROM_NAME="你的生产应用名称"

# --- 项目设置 (生产环境) ---
PROJECT_NAME="你的生产应用名称"
# 列出所有允许的前端来源地址, 用于 CORS.
BACKEND_CORS_ORIGINS='["https://your_domain.com", "https://www.your_domain.com"]'
